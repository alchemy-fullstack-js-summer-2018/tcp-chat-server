const assert = require('assert');
const ChatRoom = require('../lib/chatroom');

describe('Chat Room', () => {

    const c1 = {};
    let clients = null;

    beforeEach(() => {
        clients = new clients();
        clients.add(c1);
    });

    it('adds a user with a username', () => {
       
    });
});